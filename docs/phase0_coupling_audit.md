# Phase 0 · 상수 전수감사 결과 (시뮬 뷰포트/난수/시계 결합)

읽기전용 감사 산출물. **줄번호는 ±1 오차 가능**(편집 시 실제 매칭). 대상: 시뮬 함수 `sendArmy`(19143)~`checkWin`(20420) + 헬퍼.

> 결론: 결합 지점이 **경계된 목록**으로 확정됨. 대략 **좌표/DPR 47 · 줌 7 · 시뮬난수 15 · 시뮬시계 4**.
> **연출용(난수 13 · 시계 14)은 절대 건드리지 않는다**(§4).

## 진행 상황
- **Step 1 (완료·QA대기)** — 좌표계 기기독립화. `worldPxW/H`를 고정 기준(`SIM_VW=617, SIM_VH=1371`)으로,
  시뮬 사거리/속도/합류/점령/회피 상수의 `DPR()`→`SIM_DPR(1.5)`. **부대 위치 47개 사이트는 worldPxW/H 경유라 자동 반영.**
  `/zoom`은 유지(줌-독립은 Step 2). 에뮬레이터(dpr1.5·W≈617)에선 거의 동일 동작, 타 기기는 이 기준으로 통일.
  → 웹 QA 필요: 부대 조우/공성/점령/이동속도가 이전과 동일 체감인지.
- **Step 2 (예정)** — B 카테고리 `/zoom` 제거 → 사거리 고정 월드상수(줌-독립). 줌 시 게임성 변화 제거. QA+튜닝 필요.
- **Step 3 (예정)** — C(15) seed rng, D(4) 누적dt/틱 → 결정론 self-test.

---

## 0. 근본 원인 — 좌표계

- 부대 `army.x/y` = `정규화 × worldPxW()/worldPxH()` (= "월드픽셀" 공간).
- `worldPxW() = worldW × max(W, 360×DPR())` → **캔버스 폭 W + DPR 종속**. `worldPxH() = worldH × H`.
- 거리비교가 두 종류로 섞임: ① `dist()`=`cx/cy`(줌·카메라 포함 **스크린공간**) ② `a.x - target.x*worldPxW()`(**월드픽셀공간**, 줌 제외). 여기에 사거리는 `RANGE×DPR()/zoom`으로 비교 → **일관성 붕괴**.

**목표**: 단일 **고정 월드단위**(DPR·줌·캔버스 무관). `worldPxW/H`를 고정 기준치로, 사거리·속도는 고정 상수로, 렌더(cx/cy/w2sx/w2sy)만 줌·카메라·DPR 적용.

---

## A. 픽셀/DPR 좌표 (47) — 월드단위화 대상

**부대/사령관 위치 저장**: 19028–19029, 19034–19035(sendReinforcement) · 19094–19095(sendCombinedArmy) · 19154, 19159–19160(sendArmy) · 19282(aiTurn) · 20291–20292, 20295(arrive)
**거리계산(월드픽셀)**: 19268–19270(aiTurn) · 19383–19384(nearestCastleByOwner) · 19670–19671, 19695, 19800(resolveSieges) · 20024–20025, 20051(update)
**임계/속도/반경 상수(×DPR)**: 19017 `CATCHUP=32*DPR()` · 20106 `10*DPR()/zoom`(합류) · 20117 `12*DPR()/zoom`(점령) · 20134 `speed=18*DPR()*…/zoom` · 20150 `53*DPR()…`(성 회피) · 19017
**헬퍼 상수**: `SHOOT_RANGE=90*DPR()`(19330) · `ENGAGE_DIST=60*DPR()`(19333) · `AGGRO_DIST=100*DPR()`(19334) · `ATTACK_RANGE=80*DPR()/zoom`(19653)
**연출입자 물리(×DPR)**: 19486, 19511, 19745, 20084–20089, 20233, 20243 — *시뮬 아님이나 update 내부라 같이 정리 권장*

## B. 줌/카메라 종속 (7) — 줌 제거 대상 ⚠최우선
- `dist()`(19304) = `cx/cy` → **스크린공간(줌+카메라)** ← **최대 핫스팟, 광범위 호출**
- `d > ENGAGE_DIST()/zoom`(19532, resolveEngagements)
- `ATTACK_RANGE=…/zoom`(19653, resolveSieges 사거리)
- `aggroR = AGGRO_DIST()/zoom`(20020, update)
- `range = SHOOT_RANGE()/zoom`(20066, update)
- `speed …/zoom`(20134) · 합류/점령 `…/zoom`(20106,20117)

→ 전환: `dist`를 월드단위 `Math.hypot(a.x-b.x, a.y-b.y)`로, 사거리는 고정 월드상수(`/zoom`·`*DPR` 제거).

## C. 시뮬 난수 (15) — seed rng로 교체
- 19036 seed(sendReinforcement) · 19162 seed(sendArmy) · 19284 seed(aiTurn) · 20296 seed(arrive)
- 19234 `Math.random()<lvl.skip`(AI 스킵확률) — **AI 행동**
- 19424 tryHuoyu 성공롤(부대 소속 변경) · 19450–19451 split 각/거리 · 19462 split seed
- 20019 `_aggroCool` 스태거 · 20072 `_shootCD` 지터 · 20078–20089 화살 스폰오프셋 · 19716 `_siegeFxCD`
- 20492 checkWin 포로 장수 선택(보상)

## C-cosmetic. 연출 난수 (13) — **건드리지 말 것**
19129,19131,19182(대사) · 19372,19375(FX트리거) · 19508(오라 seed) · 19557,19566(전투대사) · 19624(사망 회전) · 19704,19706–19707(사상자 텍스트) · 19727,19730,19734–19738(입자) · 19797–19798 · 20339–20348, 20374–20387(버스트/얼룩)

## D. 시뮬 시계 (4) — 틱/누적dt로 교체
- 19313 `now - c._lastSiegeT < 700`(생산 차단) ↔ 기록처 19681 `_lastSiegeT=performance.now()`
- 19369–19370 huoyu FX 쿨다운 3500ms
- 20456 `elapsed=(performance.now()-stageStartTime)/1000`(별점 시간)

## D-cosmetic. 연출 시계 (14) — **건드리지 말 것**
19049,19172(_gateOpen) · 19488,19507,19514(huoyu FX) · 19545(hit쿨) · 19722,19847,19848(불/연기/함락 플래시) · 20074–20078(SFX 스로틀) · 20313,20314(함락 애니)

---

## 전환 전략 (권장 순서, 증분)

1. **`worldPxW()/worldPxH()`를 고정 기준치로** (예: `worldW × REF_W`, REF_W=상수). → 시뮬 좌표에서 **DPR·캔버스 W 제거를 한 곳에서** 달성. 렌더 cx/cy는 그대로 줌·카메라 적용.
2. **`dist()`를 월드단위로** + B의 `/zoom` 전부 제거, 사거리 상수에서 `*DPR()` 제거 → **고정 월드상수화**.
3. **속도/임계 상수**(20106,20117,20134,20150) `*DPR()`·`/zoom` 제거.
4. 매 단계 **싱글 QA**(부대 조우/공성/점령/속도 이전과 동일 체감). 카메라 minZoom/clampCamera 재조정 필요할 수 있음(렌더측).
5. **C(15)** seed rng로, **D(4)** 누적dt/틱으로 — 결정론 self-test.

**리스크 완화**: `feature/mp-phase0` 브랜치 + 커밋 복원점. 단계마다 커밋.
